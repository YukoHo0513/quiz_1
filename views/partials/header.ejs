<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clucks</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <nav class="border-bottom mb-4">

        <div class="big-container d-flex justify-content-between p-3">
            <a class="child-container navbar-brand" href="/clucks/index">
                <img src="/image/chicken.png" alt="chicken photo" width="60" height="54">
            </a>
            <div>
                <div class="small-container d-flex justify-content-between">
                    <div class="class-class">
                        <a class="cluck-button btn btn-secondary navbar-brand" href="/clucks">Cluck!</a>
                    </div>
                    <% if (username) {%>
                        <div class="small-container align-self-center p-2">
                            <%= username %>
                        </div>
                        <% } %>

                            <% if (username) {%>
                                <div class="small-container">
                                    <form action="/sign_out" method="POST">
                                        <button type="submit" class="sign-btn btn btn-secondary">Sign Out</button>
                                    </form>
                                </div>
                                <% } else {%>
                                    <div class="small-container d-flex justify-content-end">
                                        <form action="/sign_in" method="GET">
                                            <button class="sign-btn btn btn-secondary" type="submit">Sign in</button>
                                        </form>
                                    </div>
                                    <% } %>

                </div>


            </div>
        </div>

    </nav>